{
  "name": "calculadora",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix back\" \"npm run start --prefix front\"",
    "dev": "concurrently \"npm run dev --prefix back\" \"npm run dev --prefix front\"",
    "build": "npm run build --prefix back && npm run build --prefix front",
    "install:all": "npm install && npm install --save-dev vite --prefix front"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "body-parser": "^1.20.2",
    "concurrently": "^7.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.11.0",
    "nodemailer": "^6.9.14",
    "nodemon": "^3.1.4",
    "sweetalert2": "^11.15.10",
    "swiper": "^11.1.9"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
